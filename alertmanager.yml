global:
  resolve_timeout: 5m

route:
  receiver: "telegram"
  group_by: ["alertname"]

receivers:
  - name: "telegram"
    telegram_configs:
      - bot_token: ${TELEGRAM_BOT_TOKEN}
        chat_id: {{ .Env.TELEGRAM_CHAT_ID }} # Pass chat_id
        message: '{{ range .Alerts }}🔥 *Alert:* {{ .Annotations.summary }}\n📢 *Description:* {{ .Annotations.description }}\n🚨 *Severity:* {{ .Labels.severity }}{{ end }}'
